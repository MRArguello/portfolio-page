My portfolio page project. Features:

- prefersReducedMotion support: respects the user's reduced-motion system preference and updates on change.
- Dark mode support: theme toggle with persistent preference (defaults to dark) and system preference handling.
- Multi-language support: content is available in multiple languages via a language context.
- Email encoding: email address is obfuscated in source and decoded on the client to reduce scraping.
